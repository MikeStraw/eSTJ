// import { shallowMount } from '@vue/test-utils'
// import header from '@/components/ToolbarHeader.vue'

describe('ToolbarHeader.vue', () => {
    it ('If user is not logged in, the Log Out button is not displayed', () => {
        expect(true).toBe(true)
    })

    it('If user is logged in, the Log Out button is displayed', () => {
        expect(true).toBe(true)
    })

    it('If user is logged in, the users first name is displayed in the welcome message', () => {
        expect(true).toBe(true)
    })
})
